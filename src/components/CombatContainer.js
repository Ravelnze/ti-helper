import Container from "react-bootstrap/Container";
import Header from "./Header";

function CombatContainer(props) {
    return (
        <Container>
            <Header title="Combat" />
        </Container>
    );
}

export default CombatContainer;
