import Container from "react-bootstrap/Container";
import Header from "./Header";

function AgendaPhase(props) {
    return (
        <Container>
            <Header title="Agenda Phase" />
        </Container>
    );
}

export default AgendaPhase;
