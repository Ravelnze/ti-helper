import Container from "react-bootstrap/Container";
import Header from "./Header";

function SpaceCombat(props) {
    return (
        <Container>
            <Header title="Space Combat" />
        </Container>
    );
}

export default SpaceCombat;
