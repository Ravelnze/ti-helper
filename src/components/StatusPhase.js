import Container from "react-bootstrap/Container";
import Header from "./Header";

function StatusPhase(props) {
    return (
        <Container>
            <Header title="Status Phase" />
        </Container>
    );
}

export default StatusPhase;
