import Container from "react-bootstrap/Container";
import Header from "./Header";

function StrategyPhase(props) {
    return (
        <Container>
            <Header title="Strategy Phase" />
        </Container>
    );
}

export default StrategyPhase;
